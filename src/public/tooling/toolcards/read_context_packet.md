# Tool: read_context_packet

Use for:
- Reading the context.json file from the workspace root
- Getting ticket context and workspace metadata

Avoid when:
- You don't need the specific metadata provided by the packet

Arguments:
- None

Returns:
- Content of context.json

Example:
{"tool_name":"read_context_packet","arguments":{}}
