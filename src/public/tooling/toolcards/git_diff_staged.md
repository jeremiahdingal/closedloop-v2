# Tool: git_diff_staged

Use for:
- Showing the staged diff (changes added with git add)
- Reviewing what is ready to be committed

Avoid when:
- You want to see unstaged changes -> use git_diff

Arguments:
- None

Returns:
- Diff output

Example:
{"tool_name":"git_diff_staged","arguments":{}}
