# Tool: git_status

Use for:
- Showing the short git status of the workspace
- Seeing which files are modified, added, or deleted

Avoid when:
- You need the full diff content -> use git_diff or git_diff_staged

Arguments:
- None

Returns:
- Git status output

Example:
{"tool_name":"git_status","arguments":{}}
