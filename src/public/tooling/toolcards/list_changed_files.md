# Tool: list_changed_files

Use for:
- Listing file paths that have been modified (git diff --name-only)
- Getting a quick list of files affected by recent changes

Avoid when:
- You need the actual changes -> use git_diff

Arguments:
- None

Returns:
- List of modified file paths

Example:
{"tool_name":"list_changed_files","arguments":{}}
