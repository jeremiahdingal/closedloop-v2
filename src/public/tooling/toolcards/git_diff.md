# Tool: git_diff

Use for:
- Showing the plain workspace diff (unstaged changes)
- Reviewing what has changed but not yet added to git

Avoid when:
- You want to see staged changes -> use git_diff_staged

Arguments:
- None

Returns:
- Diff output

Example:
{"tool_name":"git_diff","arguments":{}}
